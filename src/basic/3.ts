let mixedType: string | number;
type choose = "enable" | "disable";
let select: choose;

mixedType = "Poly";
console.log(mixedType);
mixedType = 20;
console.log(mixedType);

select = "enable";
console.log(select);
